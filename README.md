# ぼうさいネコ＠平泉

岩手県平泉町の消火栓オープンデータと猫目撃情報を活用した、スマートフォン向け地域参加型・防災3DアクションWebゲームの初期リポジトリです。

この初期構成には以下を含みます。

- Vite + React + TypeScript + Zustand のベース構成
- ESLint / Prettier / Playwright の開発環境
- `/public/data/` 配下のモックJSON
- Zod によるデータバリデーション
- `localStorage` の保存処理
- 画面の基本骨組み
  - S01: ローディング
  - S02: 2D猫えらび
  - S03: 3Dみまわり（初期スキャフォールド）
  - S04: リザルト
  - S05: エラー
- 緯度経度 → ローカル座標変換関数
- Cursor 用 `.cursorrules`
- 企画・開発仕様書の `docs/` 同梱

> 注意: Leaflet（2D地図）/ Three.js（3Dみまわり）は本実装済みです。現在の実装状況は下記「機能実装状況（feature-7時点）」を参照してください。

---

## セットアップ

### 前提

- Node.js 22 系
- pnpm

### インストール

```bash
pnpm install
```

### 開発サーバ起動

```bash
pnpm dev
```

### ビルド

```bash
pnpm build
```

### 型チェック

```bash
pnpm check
```

### テスト

```bash
pnpm test
```

### Lint / Format

```bash
pnpm lint
pnpm format:check
```

### E2E（初回は Playwright ブラウザの導入が必要）

```bash
pnpm exec playwright install chromium
pnpm test:e2e
```

---

## ディレクトリ構成

```text
src/
  app/
    bootstrap/
    store/
  features/
    boot/
    map-selection/
    patrol-3d/
    result/
    error/
  components/
    ui/
  domain/
    cat/
    hydrant/
    session/
    common/
  services/
    api/
    storage/
    transform/
  styles/
public/
  data/
docs/
```

---

## 公開データ

`public/data/` 配下に以下のモックデータを配置しています。

- `cats.json`
- `hydrants.json`
- `game-config.json`
- `messages.json`

アプリ起動時にこれらを取得し、Zod でバリデーションした後に Zustand ストアへ格納します。

---

## 現在の実装方針

### MVPで優先していること

- ノンログインで即プレイできる
- データロードと状態管理の土台がある
- 仕様書に沿った型・JSON構造が揃っている
- 将来の Leaflet / Three.js 実装に差し込みやすい構造である

---

## 機能実装状況（feature-7時点）

feature-8着手にあたり、開発メンバー間で認識を合わせるために整理した一覧です。「状況」列は実際のコードを確認したうえでの実態です。

| No. | 親画面 ＞ 子画面・機能 | ユーザー視点でほしい機能 | 実装ロジック（仮説） | 状況 | 備考 |
| --- | --- | --- | --- | --- | --- |
| 1 | 選択画面 | 地図を表示 | Leafletで既存の2D地図を表示する | 完了 | |
| 2 | 選択画面＞猫選択 | 猫を選ぶ | 猫の写真付きピン | 完了 | feature-7でロック中（からだを待っている）人格をグレー表示・選択不可にする仕様を追加 |
| 3 | 選択画面＞猫選択 | 猫が行動できる範囲を表示する | ピンの座標＋半径、またはPolygonを利用して活動範囲を表示する | 完了 | |
| 4 | 選択画面＞ゲーム開始 | 初めてでも遊び方を理解したい | チュートリアル表示 | 実装前 | `session/model.ts`に`playedTutorial`フィールドのみ存在。参照・更新箇所なし |
| 5 | ゲームプレイ画面 | 3Dのフィールドを表示 | Leafletレイアウトと座標の引継ぎ | 完了 | |
| 6 | ゲームプレイ画面 | 3Dのゲーム画面でも建物を現実と同様に視覚的に理解したい | Blenderで3D造形し、座標を指定して配置 | 着手中 | 個別造形モデルは町全体メッシュ(`hiraizumi-town.glb`)とサンプル1点のみ。それ以外の建物は色付き汎用直方体で代用。feature-6でより詳細な町メッシュに差し替え済み |
| 7 | ゲームプレイ画面＞点検対象消火栓抽出 | ゲームが始まったときに、とりあえずどこに歩き出せばよいかわかる | 矢印を表示 | 実装前 | スポーン時に最寄り消火栓へ自動で向く処理のみ実装済み。`game-config.json`に`hintDelaySec`/`enableHint`はあるがコード内で未参照 |
| 8 | ゲームプレイ画面 | 自分がどちらを向いているのかを把握したい（視点の安定） | 移動ボタンを中央に配置、目・ひげのシルエット表示 | 完了 | |
| 9 | ゲームプレイ画面 | 現実の位置に消火栓があってほしい | 座標間の縮尺を維持し、ゲームのフィールドのサイズに合わせて配置 | 完了 | |
| 10 | ゲームプレイ画面 | 消火栓に近づいたら点検したい | 点検ボタン点灯 | 完了 | |
| 11 | ゲームプレイ画面 | 建物や消火栓に近づき過ぎるとぶつかり跳ね返る（突き抜けないようにする） | 当たり判定の実装、当たった時のメッセージ「あいたっ」等表示 | 完了 | |
| 12 | ゲームプレイ画面 | 点検済みか未点検か分かってほしい | 点検済みの消火栓は蓋が開く | 一部完了 | 点検前後で色を赤→緑に変える実装は完了。「蓋が開く」というモデル切り替え・開閉アニメーションは未実装 |
| 13 | ゲームプレイ画面 | 同じ消火栓を何度も加算してほしくない | 蓋が開いた消火栓は調べられない | 完了 | |
| 14 | ゲームプレイ画面 | 点検前後を見た目でも区別したい | 消火栓を配列化し、点検前後で見た目の要素（蓋の開閉）を変更 | 完了 | No.12と同じ色変化ロジック |
| 15 | ゲームプレイ画面 | あと何個点検すればいいか知りたい | ナビゲーション | 完了 | |
| 16 | ゲームプレイ画面 | 残り時間を知りたい | ナビゲーション | 完了 | |
| 17 | ゲームプレイ画面 | 制限時間内に遊びたい | 道に迷わなければ90秒以内にすべて点検できるように猫の歩行速度を調整 | 完了 | 単純な歩行速度調整ではなく、巡回経路長を最近傍法+2-opt法で推定し、90秒中70秒以内に回れるようダッシュ速度を動的算出 |
| 18 | ゲームプレイ画面 | 時間切れになったら結果を確認したい | 90秒で強制終了 | 完了 | |
| 19 | リザルト画面 | ステージ内の消火栓の何個中何個点検できたか知りたい | 定量表示 | 完了 | |
| 20 | リザルト画面 | 点検できた数がどれくらいすごい（or すごくない）のか知りたい | コメントで定性表示（よく頑張りました等） | 完了 | feature-7 で No.21 のシナリオ文言（記録の進み具合・前回比）に統合。旧 `resultLow/Mid/High` はデータ・型に残置 |
| 21 | リザルト画面＞ストーリー | 選択した猫と点検できた数によってストーリーを展開 | 条件分岐でナレーションスライド形式の画面を表示 | 完了 | feature-7。リザルト画面内の3スライド（記録→鼻→足）。分岐は `src/domain/scenario/`、文言は `messages.json` の `scenario`。全体のストーリー構造は `docs/scenario/index.html` |
| 22 | 選択画面 | 同一セッション内でのプレイ結果を反映 | 同一セッション内で結果を保持 | 着手中 | `localStorage`への永続化と前回選択猫の自動復元は実装済み。選択画面UIへの結果の視覚的な反映は未実装 |

---

## 主要スクリプト

| コマンド            | 内容                  |
| ------------------- | --------------------- |
| `pnpm dev`          | 開発サーバ起動        |
| `pnpm build`        | 本番ビルド            |
| `pnpm preview`      | ビルド確認            |
| `pnpm check`        | TypeScript 型チェック |
| `pnpm lint`         | ESLint                |
| `pnpm format:check` | Prettier チェック     |
| `pnpm test`         | Vitest 実行           |
| `pnpm test:e2e`     | Playwright E2E        |

---

## 仕様書同梱

- `docs/ぼうさいネコ_更新版仕様書.md`
- `docs/ぼうさいネコ_開発仕様書.md`

---

## ライセンス

ライセンスは未確定です。公開前に決定してください。
